{
  "name": "@claw/cron",
  "version": "0.1.0",
  "description": "node-cron wrapper — loads scheduled jobs from SQLite, fires skill-runner",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@claw/memory": "*",
    "@claw/skill-runner": "*",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.4.5"
  }
}
